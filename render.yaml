databases:
  - name: flashsalepro-db-sg
    plan: free
    region: singapore
    databaseName: flashsalepro

services:
  - type: web
    name: flashsalepro
    runtime: ruby
    plan: free
    region: singapore
    buildCommand: "./bin/render-build.sh"
    # Migrations run on container start because the build phase has no
    # access to the private DB network. db:prepare is idempotent.
    startCommand: "bundle exec rails db:prepare && bundle exec rails server"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: flashsalepro-db-sg
          property: connectionString
      # Auto-generated on first deploy; rotated by recreating the service.
      - key: SECRET_KEY_BASE
        generateValue: true
      - key: RAILS_LOG_TO_STDOUT
        value: "true"
      - key: RAILS_SERVE_STATIC_FILES
        value: "true"
      # Free tier has 512MB RAM; single Puma worker keeps memory in budget.
      - key: WEB_CONCURRENCY
        value: "1"
