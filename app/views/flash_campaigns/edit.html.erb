<div class="max-w-2xl mx-auto p-8">
  <div class="mb-8">
    <%= link_to "← 返回活動", flash_campaign_path(@flash_campaign), class: "text-sm text-gray-500 hover:text-gray-700" %>
    <h1 class="text-3xl font-bold text-gray-900 mt-2">編輯活動</h1>
    <p class="text-gray-500 mt-2">調整定價、庫存或截止時間以呼應 AI 的建議。</p>
  </div>

  <div class="bg-white rounded-2xl shadow-sm border border-gray-100 p-8">
    <%= render "form" %>
  </div>
</div>
